@Component
script ModeStartUI extends Component

	@EventSender("Self")
	handler HandleButtonClickEvent(ButtonClickEvent event)
		-- MatrixGameManager를 통해 게임 시작
		if _MatrixGameManager ~= nil then
			_MatrixGameManager:StartMatrixGame()
		end
	end

end