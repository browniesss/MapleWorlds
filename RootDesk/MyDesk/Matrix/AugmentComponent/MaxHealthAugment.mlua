@Component
script MaxHealthAugment extends BaseMatrixAugmentComponent

	method void OnSpawn()
		local unit = self.Entity.Unit
		if unit == nil then
			return
		end
		
		-- MaxHealthMultiplyBuff는 배율 그대로 전달
		unit:GetBuff(_eBuffType.MaxHealthMultiplyBuff, self.value, 0)
	end

end