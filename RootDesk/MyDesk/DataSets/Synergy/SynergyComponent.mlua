@Component
script SynergyComponent extends Component

	@ExecSpace("ServerOnly")
	method void APPLY_UNIT_SYNERGY(EntityRef arg1)
		local count = list:length()
		
		for i=1, count do
			local item = list:Get(i)
			self.Entity.Unit:GetBuff()
		end
	end

end